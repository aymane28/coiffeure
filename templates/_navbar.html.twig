<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('accueil') }}">Confort Zone</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor02">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <!--<a class="nav-link active" href="#">Salons de coiffure-->
            <a class="nav-link" href="{{ path('salons') }}">Salons de coiffure
            <!--<span class="visually-hidden">(current)</span>-->
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Mains & Pieds</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Boutique</a>
        </li>
      </ul>
    </div>
    <ul class="navbar-nav me-auto">
      <li class="nav-item">
        <a class="nav-link" href="{{ path('app_register')}}">Ajouter votre établissement</a>
      </li>
      <li class="nav-item">
        <!--<a class="nav-link active" href="#">Salons de coiffure-->
        {% if app.user %}
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"> <span class="fa fa-user"></span> {{ app.user.email }}</a>
        <div class="dropdown-menu">
          <a class="dropdown-item" href="#"><i class="bi bi-bag-check"></i> Mes rendez-vous</a>
          <a class="dropdown-item" href="#"><i class="bi bi-gear"></i> Paramètres</a>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href=" {{ path('app_logout') }}"><i class="bi bi-door-open"></i>  Se déconnecter</a>
        </div>
        {% else %}
        <a class="nav-link" href="{{ path('app_login') }}"> <span class="fa fa-user"></span> Mon compte
        {% endif %}

          <!--<span class="visually-hidden">(current)</span>-->
        </a>
      </li>
    </ul>
  </div>
</nav>